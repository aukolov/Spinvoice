<Window x:Class="Spinvoice.Views.Exchange.ExchangeRatesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:exchange="clr-namespace:Spinvoice.ViewModels.Exchange"
        xmlns:exchange1="clr-namespace:Spinvoice.Views.Exchange"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="Exchange Rates"
        d:DataContext="{d:DesignInstance Type=exchange:ExchangeRatesViewModel}"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        mc:Ignorable="d">
    <StackPanel Margin="10" Orientation="Vertical">
        <exchange1:LoadExchangeRatesView DataContext="{Binding LoadExchangeRatesViewModel}" />
        <exchange1:CheckExchangeRatesView Margin="0,10,0,0" DataContext="{Binding CheckExchangeRatesViewModel}" />
        <Button Width="70"
                Margin="0,10,0,0"
                HorizontalAlignment="Right"
                Command="{Binding CloseCommand}"
                Content="Close" />
    </StackPanel>
</Window>
